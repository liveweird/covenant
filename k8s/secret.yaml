# TEMPLATE — do NOT apply with these placeholder values (the app refuses to start on the
# placeholder JWT secret). Create the real secret out-of-band and never commit it:
#
#   kubectl create namespace covenant
#   kubectl -n covenant create secret generic covenant-secrets \
#     --from-literal=JWT_SECRET="$(openssl rand -hex 32)" \
#     --from-literal=POSTGRES_USER=covenant \
#     --from-literal=POSTGRES_PASSWORD="$(openssl rand -hex 16)" \
#     --from-literal=ADMIN_INITIAL_PASSWORD='<choose-the-first-admin-password>' \
#     --from-literal=CHECKER_TOKEN="$(openssl rand -hex 16)"
#
# ADMIN_INITIAL_PASSWORD rotates the seeded admin@covenant.local account away from the
# well-known 'changeme' on first boot; without it, production mode refuses to start.
apiVersion: v1
kind: Secret
metadata:
  name: covenant-secrets
  namespace: covenant
type: Opaque
stringData:
  JWT_SECRET: CHANGE-ME-openssl-rand-hex-32
  POSTGRES_USER: covenant
  POSTGRES_PASSWORD: CHANGE-ME
  ADMIN_INITIAL_PASSWORD: CHANGE-ME
  CHECKER_TOKEN: CHANGE-ME
