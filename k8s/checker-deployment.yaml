apiVersion: apps/v1
kind: Deployment
metadata:
  name: checker
  namespace: covenant
  labels:
    io.kompose.service: checker
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: checker
  template:
    metadata:
      labels:
        io.kompose.service: checker
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: covenant-checker
          # Built locally with `docker build -t covenant-checker:latest -f checker/Dockerfile .`;
          # OrbStack's Kubernetes shares the Docker image store (see app-deployment.yaml).
          image: covenant-checker:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "9090"
            - name: CHECKER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: covenant-secrets
                  key: CHECKER_TOKEN
                  optional: true
          ports:
            - containerPort: 9090
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9090
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9090
            periodSeconds: 15
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
